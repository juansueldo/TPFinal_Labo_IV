import { DiaStringPipe } from './dia-string.pipe';

describe('DiaStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaStringPipe();
    expect(pipe).toBeTruthy();
  });
});
