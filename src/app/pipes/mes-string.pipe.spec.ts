import { MesStringPipe } from './mes-string.pipe';

describe('MesStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MesStringPipe();
    expect(pipe).toBeTruthy();
  });
});
