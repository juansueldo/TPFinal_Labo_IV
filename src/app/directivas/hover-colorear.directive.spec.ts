import { HoverColorearDirective } from './hover-colorear.directive';

describe('HoverColorearDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColorearDirective();
    expect(directive).toBeTruthy();
  });
});
